import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.houseofguidance.chat',
  appName: 'House of Guidance Chat',
  webDir: 'dist'
};

export default config;
